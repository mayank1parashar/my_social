flask
flask-socketio
flask-cors
bcrypt
PyJWT
Pillow
python-dotenv
# eventlet is excluded for serverless deployments; enable Socket.IO on a container host if needed
# Add `eventlet` back when running in a long-lived server and set ENABLE_SOCKETIO=1
